# Android Automation

Automation currently uses ADB because no standalone `android` CLI is installed in this environment.

Planned coverage:

- device probe
- Bluetooth pairing flow
- media playback routing checks
- helper app for mic capture and synchronized AV playback
